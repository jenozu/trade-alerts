# Optional morning-report narrative

Use only the supplied deterministic morning-alert JSON. Do not calculate, add,
alter, round, or infer prices, levels, stops, targets, bias, or confirmations.
If a value is absent, say it is unavailable. This narrative is optional and
must never replace the deterministic JSON or Markdown report.
