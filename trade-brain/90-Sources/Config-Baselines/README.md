# Config Baselines

Pointers to strategy/session configuration baselines used by research experiments.

Canonical live files remain in the repository `config/` directory. When an experiment depends on a specific config state, record its Git SHA and relevant file hash in the experiment note or immutable research-run archive.
